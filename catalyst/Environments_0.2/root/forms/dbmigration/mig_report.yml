---
# Indicator is hte field used to test for form submission
indicator: submit
# Start listing the form elements
elements:
     
    # A drop down for ENV2 - dynamically filled from controller
   - type: Select
     name: env
     label: Environment
   
   - type: Select
     name: rel
     label: Release
     
   # Submit Button
   - type: Submit
     name: submit
     value: Show Migration Report