build.classes.dir=${build.web.dir}/WEB-INF/classes
build.classes.excludes=**/*.java,**/*.form
build.dir=build
build.generated.dir=${build.dir}/generated
build.web.dir=${build.dir}/web
#build.web.dir=G:/Inetpub/Tomcat/5.5/webapps/exam
build.web.excludes=${build.classes.excludes}
client.urlPart=
compile.jsps=false
debug.classpath=${javac.classpath}:${build.classes.dir}
display.browser=true
dist.dir=dist
dist.javadoc.dir=${dist.dir}/javadoc
dist.war=G:/Inetpub/Tomcat/5.5/webapps/${war.name}
#dist.war=${dist.dir}/${war.name}
j2ee.platform=1.3
j2ee.server.type=Tomcat5
jar.compress=false
javac.classpath=${libs.servlet23.classpath};${libs.Secretary.classpath};${libs.DbConnectionBroker.classpath};${libs.jtds.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.debug=true
javac.deprecation=false
javac.source=${default.javac.source}
javac.target=${default.javac.source}
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.preview=true
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
lib.dir=src/WEB-INF/lib
no.dependencies=false
platform.active=default_platform
# Space-separated list of JVM arguments used when running class with main method
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value):
runmain.jvmargs=
source.root=.
src.dir=src/WEB-INF/classes
war.content.additional=
war.name=exam.war
web.docbase.dir=src
